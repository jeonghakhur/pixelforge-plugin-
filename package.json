{
  "name": "pixelforge-plugin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "@types/jszip": "^3.4.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
